# Health Staffing – Fetch Users

## What it does
Fetches users from `https://jsonplaceholder.typicode.com/users`, transforms to:
`{ id, name, email, company }`, and saves to `users.json`.

## How to run
```bash
npm install
npm run fetch-users
# output: users.json
